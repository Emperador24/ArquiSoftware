.DS_Store

# Artefactos de compilación de LaTeX (Work/ solo debe versionar el .tex fuente)
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# Los PDF compilados van SIEMPRE en Submission/ (solo si el entregable ya está
# terminado) o Defense/, nunca en Work/ — ver CLAUDE.md. Evita que quede un
# PDF de borrador rastreado ahí por accidente al compilar con pdflatex.
Proyecto/Documentation/Work/*.pdf
